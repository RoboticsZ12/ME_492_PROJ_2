#include <stdio.h>
#include <iostream>
#include "MATH_A_LIB.hpp"
using namespace std;

int main()
{
    math_lib add_mult;
    add_mult.addition(5,10);
    add_mult.multiplication(10,8);

    return(0);
}