// THIS IS STRICTLY A PRACTICE CODE FILE. 


//#pragma once
//#include <opencv2/opencv.hpp>
//#include <iostream>


//class open_cv
//{
    // Create a 200x200 pixel image with 3 channels (RGB) and initialize it with white color
  //  cv::Mat image(200, 200, CV_8UC3, cv::Scalar(255, 255, 255));

    // Display the image in a window titled "Test OpenCV"
   // cv::imshow("Test OpenCV", image);

    // Wait for any key press indefinitely
   // cv::waitKey(0);

    // Return 0 to indicate successful execution
   // return 0;
//}